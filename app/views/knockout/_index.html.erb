
<header class="search-form picture-background" data-bind="template: { name: 'search', data: currSearch }"></header>
<main class="results" data-bind="visible: !initialView(), template: { name: 'results', data: lastSearch }"></main>

<%= render partial: 'knockout/search' %>
<%= render partial: 'knockout/tag' %>
<%= render partial: 'knockout/results' %>
<%= render partial: 'knockout/preview' %>