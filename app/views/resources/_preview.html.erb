<%= content_tag :div, '', class: "#{preview.type.downcase}-wrapper resource-wrapper" do %>
	<%= image_tag preview.avatar, class: 'resource-image' %>
	<%= link_to "  #{preview.name} »", preview.show_path %>
<% end %>
